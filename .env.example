# Where a job mints credentials of its own, since the ones it is started with
# lapse while it is still running. The token itself is held by the platform as a
# secret named DHCORE_PERSONAL_ACCESS_TOKEN, and never lands here.
DHCORE_ISSUER=https://core.your-digitalhub-endpoint
DHCORE_CLIENT_ID=
